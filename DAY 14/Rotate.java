class RotateImage {
public void Rotate(int[][] matrix) {
        int n = matrix.length;
        for(int i=0;i<n-1;i++){
            for(int j=i+1;j<n;j++){
                //swap(matrix[i][j], matrix[j][i]);
            }
        }
        for(int i=0;i<n;i++){
            //reverse(matrix[i]-start, matrix[i]-end());
        }
       
    }
};